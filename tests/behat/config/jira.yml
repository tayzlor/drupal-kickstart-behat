# jira.yml
default:
  extensions:
    VIPSoft\JiraExtension\Extension:
      jql: "summary ~ 'Kickstart Feature'"
      comment_on_pass: false
      comment_on_fail: false
      reopen_on_fail: false
      cache_directory: /tmp/behat-jira

imports:
  - jira.credentials.yml
